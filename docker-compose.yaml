services:
  docked:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docked
    restart: unless-stopped
    ports:
      - "3002:3002"
    environment:
      - PORT=3002
      - DATA_DIR=/data
      - TZ=America/Chicago
    volumes:
      - ./data:/data
    networks:
      - docked-network

networks:
  docked-network:
    driver: bridge
