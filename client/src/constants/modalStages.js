/**
 * Modal Stage Constants
 * Standardized stage values for modals with multiple states
 */

export const MODAL_STAGES = {
  CONFIRM: "confirm",
  PROGRESS: "progress",
  RECONNECTING: "reconnecting",
  ERROR: "error",
};

export const UPGRADE_MODAL_STAGES = MODAL_STAGES;
